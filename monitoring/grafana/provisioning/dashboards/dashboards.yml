apiVersion: 1

providers:
  - name: CreditScoring
    orgId: 1
    folder: Credit Scoring
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
