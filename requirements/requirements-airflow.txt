# Extra packages ONLY (no apache-airflow, no sqlalchemy pins).
# Provider is installed separately in Dockerfile.airflow with Airflow constraints.
# Base image: apache/airflow:2.8.1-python3.11 already includes Airflow + alembic.

# ML (training DAGs)
catboost==1.2.3
lightgbm==4.3.0
xgboost==2.0.3
scikit-learn==1.4.0
optuna==3.6.1

# Data (flexible to avoid fighting base image pins)
pandas>=2.0,<2.3
numpy>=1.24,<1.27
scipy>=1.10,<1.13

# MLflow tracking client (server is a separate container)
mlflow==2.14.3
psycopg2-binary==2.9.9
sqlalchemy==2.0.25

# Explainability
shap==0.44.1

# Utils
python-dotenv==1.0.1
pyyaml>=6.0.1
loguru==0.7.2
requests==2.31.0

# Tests inside container
pytest==8.0.0
